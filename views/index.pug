<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{pageName}
        link(rel="stylesheet", href="/css/index.css")
        link(rel='shortcut icon' type='image/x-icon' href='/images/favicon.png')
        script(src='/scripts/jquery-3.4.1.js')
        script(src='/scripts/deviceSearch.js')
    body
        b Nexthink Checkit PC
        .col-12.doc-page-section.row
            .col-12
                img(src="./images/acme.png")
                p Enter the name of the position to search, or just a character sequence
                p There is no need to use wildcards like *
                input.col-5(id='myInput' name='searchBox')
                button.button(type='text' name='search') Search
            .col-5
                ul(id="devicesList")
